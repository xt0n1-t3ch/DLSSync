# Application composition

This layer owns startup, routing, providers, and composition only. Product rules stay in `features`, domain shapes in `entities`, reusable UI and transport in `shared`, and generated Rust contracts in `generated`.
